# Python 3.12 environment validated on GTX 1650 Ti / driver 595.84.
# `torch` resolves to the CUDA wheel on Linux; verify with tools/cuda_smoke.py.
torch==2.14.0
torchvision==0.29.0
transformers==5.17.0
accelerate==1.15.0
# The live UI is direct OpenCV; no browser UI transport is required.
opencv-python-headless==5.0.0.93
numpy==2.5.3
pillow==12.3.0
psutil==7.2.2
pytest==9.1.1
playwright==1.62.0
# Mode 7 OpenGL 3.3 core renderer.
glfw>=2.7.0
PyOpenGL>=3.1.7
# Colleague hand-control integration. The .task model asset stays outside Git.
mediapipe>=1.0.1

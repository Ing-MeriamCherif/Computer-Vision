# Person 1 Phase-1 baseline (hand-only). No torch needed yet.
numpy>=1.26
opencv-python>=4.8
mediapipe>=0.10
PyYAML>=6.0
Pillow>=8.0
# Optional: YOLO backend (HAND_BACKEND=yolo). Needs torch; cuda on 4060.
ultralytics>=8.0
# Phase 2: depth backends (depth/ package) + D2NT normals
torch>=1.8
transformers>=4.40
# Phase 2: D2NT depth->normals (normal_translator.py, tools/bench_normals.py)
d2nt>=0.1.0
# Phase 4 renderer (renderer/ ModernGL) + full_pipeline.py
moderngl>=5.0
glfw>=2.0
